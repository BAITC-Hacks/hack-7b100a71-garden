# mock: isolated demo fixtures. real: centralized HTTP adapter.
VITE_API_MODE=mock
# Set the externally running backend base URL when using real mode (no /api suffix).
# No HTTP calls are made in mock mode. Do not put Bearer or AI secrets in VITE_*.
VITE_API_BASE_URL=
